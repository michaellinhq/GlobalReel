# apps/api TODO

- [ ] NestJS AppModule + Fastify adapter.
- [ ] Postgres via Prisma, Redis (BullMQ), MinIO client.
- [ ] Auth module (JWT + tenant scope).
- [ ] Billing module (Stripe) — owned by `revenue-ops`.
- [ ] Gateway WebSocket (mTLS) endpoint.
- [ ] OpenAPI spec → packages/shared-types generation.
- [ ] Compliance pre-flight middleware (calls packages/compliance).
