[project]
name = "globalreel-bi-worker"
version = "0.0.0"
description = "Trend scraping and forecasting (Polars + DuckDB)."
requires-python = ">=3.12"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.mypy]
python_version = "3.12"
strict = true
