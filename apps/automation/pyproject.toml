[project]
name = "globalreel-automation"
version = "0.0.0"
description = "Appium device automation for the Android + SIM swarm."
requires-python = ">=3.12"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.mypy]
python_version = "3.12"
strict = true
